// Attach TestPartModule to every command pod so any launched vessel shows
// the "Hot Reload Test" field in its PAW. Requires ModuleManager.
@PART[*]:HAS[@MODULE[ModuleCommand]]:FOR[HotReloadKSP_Test]
{
    MODULE
    {
        name = TestPartModule
    }
}
